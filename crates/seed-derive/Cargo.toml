[package]
name = "seed-derive"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

# `#![no_std]`, no `alloc` (SPEC §9, §13).
[dependencies]
seed-core = { path = "../seed-core" }
k256 = { workspace = true }
zeroize = { workspace = true }
