[package]
name = "seed-test-vectors"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

# Test-only helper crate (SPEC §9, §28: never in the production graph).
# Depends only on sibling workspace crates — no third-party additions
# (IMPLEMENTATION_MAP.md §1 rule 5). `seed-uefi-production` MUST NOT
# depend on this crate (SPEC §9, §28); verify with
# `cargo tree -p seed-uefi-production` (ci.sh).
[dependencies]
seed-core = { path = "../seed-core" }
seed-protocol = { path = "../seed-protocol" }
seed-derive = { path = "../seed-derive" }
