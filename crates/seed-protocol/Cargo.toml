[package]
name = "seed-protocol"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

# `#![no_std]`, no `alloc`.
[dependencies]
seed-core = { path = "../seed-core" }
zeroize = { workspace = true }
