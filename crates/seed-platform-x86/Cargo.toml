[package]
name = "seed-platform-x86"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish = false

# `#![no_std]`, no `alloc`. UEFI-only platform code: pulls in `uefi`
# directly (SPEC §31 allows UEFI protocol access).
[dependencies]
seed-core = { path = "../seed-core" }
seed-protocol = { path = "../seed-protocol" }
uefi = { workspace = true }
